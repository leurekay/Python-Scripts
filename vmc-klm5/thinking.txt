10.31：
#在计算hopping相关的ratio时，技巧和蛮力一致
#但在计算spin-exchange相关的ratio时，两者不一致。
#计算ratio_spinflip 有两种方法。
	1。直接计算spin-flip 前后两个行列式的比值，
	2.局域电子和巡游电子分别hop到相反的格位，

	两个hop的ratio相乘。
发现，1和2会相差一个负号。我比较倾向2正确

11.02：
#算误差的时候还要额外除(N-1)
#误差算出来为什么偶尔会出现负数？
  ---也许是计算中出现了复数值
#L=2 

11.03：
#算的的误差σ是不同采样系宗的。
针对固定的J，尺寸，变分参数，我今天算一次，明天算一次，后天算一次。。。。。
每次都计算出能量的值，算了一万天，σ即是10000个样本能量的variance。
但我要想通过一次计算就求得σ，有什么办法呢？
	---通过公式计算，可以转化成计算一次时E_local的variance
	var(<E>)=var(E_local)/N

#卧槽！！！！ 不会是要调用E_loc_spin3吧！！！！
之前调用E_loc_spin1，误差有点大
#今天，总能量的误差总算控制在了0.005左右，但是速度还有点慢。
测量5000次得出结果也还可以
#dyson 链式反应似乎有点想通了，但写成代码需要大动干戈

11.7：
#在计算自旋翻转相关的行列式比值时，除了可以直接计算行列式比值外，还可以间接的计算。
之前所用的间接计算错误，现阶段似乎正确的做法是：
D电子跳到相同自旋C电子，C电子跳到相同自旋D电子，间接计算行列式比值(函数Ratio_exchange2)
但是这么做会和Ratio_exchange偶尔产生一个负号，在更新构型的时候，这个负号无所谓，毕竟是要平方的，
但在计算可观测量的局域值时，就纠结了！！！！！！！！！！！！！！！！！
计算了一下总能量，发现Ratio_exchange2要比Ratio_exchange数值大一点，似乎更正确

11.10：
#dyson-chain-update成功，但是代码乱的像砣翔
似乎没有完全成功，能量有时会发散